### L. 第五切片或介面重構任務書

```text
任務名稱：0728–0729 可測試介面與狀態重構
選擇：有證據的介面重構

需求與模型版本：BASELINE-2026-0722-V1；0727 OO Analysis
原型版本：midterm-report-site／基準提交 59fa6ff
目前實作版本：0728–0729 工作版本

目前症狀：既有核心流程可操作，但缺少送出載入、處理中停用、權限不足、欄位級錯誤關聯及有效前端測試；Java 後端可接受未定義狀態回轉。
可重現步驟：送出有效表單會立即完成；自由切換角色不會顯示權限狀態；執行 npm test 會測試不存在的 starter skeleton；核准後呼叫 updateStatus 可再改待補件。
模型與畫面依據：SEQ-01、SEQ-02、ST-01～ST-06、IST-01、UI-01～UI-10。

必須保留：FR-01～FR-07 核心表單、衝突判斷、待審核紀錄、補件與角色查詢示範。
必須新增或修正：三個核心任務說明；匿名測試資料；載入、停用、權限不足；欄位錯誤關聯；非法狀態轉移拒絕；實際專案測試。
本次不做：正式登入、資料庫、Email／LINE、正式簽核層級、真人觀察代填。
不可自行決定：FR-08～FR-10 正式規則、撤銷核准、跨學期場地優先權、正式部署平台。

正常狀態：可建立申請、查詢狀態、處理待審核資料。
替代狀態：相鄰時段可送出；待補件可取消修改或重送。
錯誤狀態：缺欄位、時間順序錯誤、場地衝突、缺審核原因、非法狀態轉移。
停用或權限狀態：送出與審核處理中停用；可預覽權限不足並返回。

主要測試：有效申請建立且狀態為待審核。
替代測試：相鄰時段允許、待補件 revision+1。
錯誤測試：缺欄位、重疊時段、缺原因。
狀態測試：待審核→待補件→待審核→已核准；拒絕已核准回轉。
回歸測試：建置、lint、前端 domain、SSR、Java ApplicationServiceTest。

完成後回報：
1. 修改檔案與理由。
2. 需求、模型與畫面對照。
3. 實際執行的測試與結果。
4. 未通過項目與剩餘限制。
5. 不得把未執行測試寫成通過。
```
