### O. 跨模型一致性矩陣

| 情境或規則                       | 需求                   | 使用案例              | ERD（實體關係圖）                             | Class（類別圖）                                                                   | Sequence（循序圖）     | State（狀態圖）                            | 實作                                              | 測試                                   | 結論                                                         |
| -------------------------------- | ---------------------- | --------------------- | --------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------- | ------------------------------------------ | ------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------ |
| 主要流程：有效且無衝突的活動申請 | FR-01、FR-02、FR-05    | UC-01、UC-03 主要流程 | `activity_applications`、`venue_bookings` | ApplicationFormBoundary→ApplicationController→ActivityApplication／VenueBooking | SEQ-01 訊息 1～9       | ST-01 初始節點→待審核                     | React`submitApplication`；Java `create`       | MST-01、Java`ApplicationServiceTest` | 一致                                                         |
| 例外流程：缺少必填欄位           | FR-02、NFR-01          | UC-01 步驟 3 例外     | `activity_applications` 必填屬性            | ApplicationController.validate()                                                  | SEQ-01 訊息 4 回傳錯誤 | 不建立 ActivityApplication 持久狀態        | React 142～147；Java 363～371                     | EXT-01、既有 Java 測試                 | 一致                                                         |
| 例外流程：同場地同日重疊         | FR-03、FR-04、BR-01    | UC-02／UC-01 E-01     | `venue_bookings` 日期與時段                 | VenueBooking.overlaps()、ApplicationController                                    | SEQ-01 訊息 5～6       | 不建立 ActivityApplication 持久狀態        | React 158～173；Java 322～332                     | EXT-02、既有 Java 測試                 | 一致，但規則在前後端重複                                     |
| 替代流程：相鄰時段可送出         | FR-03、FR-04、AC-03-02 | UC-02 A-01            | `venue_bookings` 起訖時間                   | VenueBooking.overlaps() 使用半開區間                                              | SEQ-01 訊息 6          | ST-01 初始節點→待審核                     | 使用 start < existingEnd 且 end > existingStart   | EXT-03 尚缺既有自動測試                | 部分一致；實作成立但測試缺漏                                 |
| 狀態轉移：新申請為待審核         | FR-05、FR-06、BR-03    | UC-03                 | `activity_applications.status`              | ActivityApplication.submit()                                                      | SEQ-01 訊息 7～9       | ST-01 初始節點→待審核                     | React 204～215；Java 212～230                     | ST-01、既有 Java 測試                  | 一致                                                         |
| 替代流程：要求補件後重送         | FR-08；補件規則待確認  | UC-06 A-01／A-02      | `status`、reviewNote／審核屬性未完整定義    | ReviewController、ActivityApplication.resubmit()                                  | SEQ-02 訊息 2～8       | ST-03 待審核→待補件；ST-04 待補件→待審核 | React 224～290；Java 233～319                     | ST-03、ST-04、既有 Java 測試           | 部分一致；實作有行為，0722 ERD 未完整列 reviewNote、revision |
| 狀態轉移：待審核核准             | FR-08（待確認）        | UC-06                 | `activity_applications.status`、REL-04      | ReviewController.review()、ActivityApplication.changeStatus()                     | SEQ-02 alt 分支 1      | ST-02 待審核→已核准（待確認）             | React 249～289；Java 233～269                     | ST-02、既有 Java 測試                  | 實作與模型相符，但需求 FR-08 尚待確認                        |
| 未定義轉移：已核准再改待補件     | 撤銷／回轉規則待確認   | UC-06 未定義          | `status` 允許值存在，轉移限制未確認         | ReviewController.validateTransition()                                             | SEQ-02 訊息 3 應先檢查 | IST-01 確認前拒絕未定義轉移                | Java`updateStatus` 只檢查目標值，不檢查目前狀態 | IST-01 程式碼檢查失敗                  | 不一致；先確認 FR-08，再決定實作與模型修正                   |

### P. 一致性問題紀錄

| 問題編號 | 症狀                                                                                                                    | 模型依據                                      | 最可能原因                                                                      | 修正位置                                                                                     | 驗證方式                                                             | 狀態   |
| -------- | ----------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | ------ |
| OO-01    | Java 後端可把已核准申請再改成待補件或已退回，但需求未定義此回轉是否允許                                                   | N 表 IST-01；FR-08 尚待確認                  | 狀態轉移規則未確認，`ApplicationService.updateStatus` 也未檢查目前狀態        | 先確認 FR-08；確認禁止後才修改 `ApplicationService.updateStatus` 與對應測試              | 建立→核准後再要求補件，依確認規則驗證結果且原資料不可被意外改動         | 待確認 |
| OO-02    | 必填、時間與衝突規則同時寫在`Prototype` 與 `ApplicationService`，ActivityApplication／VenueBooking 只有資料沒有行為 | D、E、G 的 Entity 責任；BR-01～BR-03          | 原型由單一 React 元件快速完成，後端再獨立複製規則                               | 模型與實作同步：將狀態規則交給 ActivityApplication，重疊規則交給 VenueBooking 或共用領域服務 | 前後端共用同一組邊界案例；重構前後既有核心測試皆通過                 | 待修正 |
| OO-03    | AC-03-02 相鄰時段與兩筆非法轉移缺少完整自動測試；現有測試未涵蓋核准後回轉                                               | AC-03-02、N 表 IST-01～IST-02、任務 11        | 既有`ApplicationServiceTest` 聚焦主要流程、衝突、補件與必填，狀態轉移覆蓋不足 | 先補測試：`ApplicationServiceTest`；測試證據記入 T、U                                      | 執行主要 2、替代／例外 2、合法狀態 3、非法狀態 2；保留失敗與重測結果 | 待補測 |
